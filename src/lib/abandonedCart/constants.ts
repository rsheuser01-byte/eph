export const CART_SESSION_COOKIE = "eph_cart_sid";

/** Default retention for saved carts and restore tokens. */
export const SAVED_CART_TTL_MS = 30 * 24 * 60 * 60 * 1000;

export const SAVED_CART_CURRENCY = "USD";

export const IDENTIFIED_IDEMPOTENCY_PREFIX = "abandoned-cart:";
